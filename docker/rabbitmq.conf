# A VU job stays unacknowledged for the whole test (it is acked when the VU finishes),
# so raise the delivery-ack timeout well above the longest test you plan to run (24h).
consumer_timeout = 86400000
